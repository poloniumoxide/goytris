#include "force.h"

using namespace std;


    vector<vector<int>> f;

    int length;
    int strength;

    Force::Force(int str, int entr) {
        //add this into f
    }

    void Force::merge(Force f) {

    }

    void Force::cancel(Force f) {

    }

    //diminish, boost

