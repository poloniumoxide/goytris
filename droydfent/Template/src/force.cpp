#include <vector>

using namespace std;

class Force {
public:

    vector<vector<int>> f;

    int length;
    int strength;

    Force(int str, int entr) {
        //add this into f
    }

    void merge(Force f) {

    }

    void cancel(Force f) {

    }

    //diminish, boost

};